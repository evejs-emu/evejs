# Third-Party Notices

This repository uses open-source dependencies listed in `package.json`,
`server/package.json`, and tool-specific package manifests.

The repository does not license or redistribute EVE Online client files, CCP
static data exports, CCP assets, or patched CCP binaries. Users must obtain the
EVE Online client and any public static data directly from CCP-controlled
distribution channels.

Node.js and npm dependencies are installed locally by `SetupEveJS.bat`.
